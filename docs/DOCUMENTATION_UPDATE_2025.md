# Обновление документации ELOM - Октябрь 2025

## Дата: 08 Октября 2025
## Версия: 3.0

---

## 📊 АКТУАЛЬНАЯ СТАТИСТИКА ПРОЕКТА

### Frontend компоненты
- **Vue компоненты**: 46 файлов (включая 9 мобильных карточек)
- **Stores (Pinia)**: 16 stores
- **Composables**: 11 композаблов
- **Страницы**: 25+ страниц
- **API типы**: 15 модульных файлов
- **Утилиты**: 11 утилит

### Компоненты по категориям

#### Универсальные компоненты (6):
1. `GenericForm.vue` - Универсальная система форм
2. `GenericList.vue` - Универсальная система списков
3. `FormField.vue` - Базовые поля форм
4. `GenericField.vue` - Расширенные поля форм
5. `GenericFormField.vue` - Специализированные поля
6. `Modal.vue` - Модальные окна

#### Списки и таблицы (8):
1. `Table.vue` - Базовая таблица
2. `AnimatedTable.vue` - Анимированная таблица
3. `TableSkeleton.vue` - Скелетон таблицы
4. `CardSkeleton.vue` - Скелетон карточки
5. `ListHeader.vue` - Заголовок списка
6. `Pagination.vue` - Пагинация
7. `ModernPagination.vue` - Современная пагинация
8. `ExportButton.vue` - Кнопка экспорта

#### Фильтры и поиск (4):
1. `FilterPanel.vue` - Панель фильтров
2. `FilterField.vue` - Поля фильтров
3. `MaterialSearchSelect.vue` - Поиск материалов
4. `SupplierSearchSelect.vue` - Поиск поставщиков

#### Навигация (4):
1. `AutoNavigation.vue` - Автоматическая навигация
2. `AutoMobileNavigation.vue` - Мобильная навигация
3. `MobileNavigation.vue` - Мобильная навигация
4. `AppSidebar.vue` - Боковая панель

#### Мобильные карточки (9):
1. `MaterialCard.vue` - Карточка материала
2. `PurchaseCard.vue` - Карточка закупки
3. `StockCard.vue` - Карточка движения
4. `SupplierCard.vue` - Карточка поставщика
5. `EmployeeCard.vue` - Карточка сотрудника
6. `ObjectCard.vue` - Карточка объекта
7. `UnitCard.vue` - Карточка единицы измерения
8. `WriteOffCard.vue` - Карточка списания
9. `MaterialCategoryCard.vue` - Карточка категории

#### UI компоненты (8):
1. `LoadingSpinner.vue` - Индикатор загрузки
2. `BrowserWarning.vue` - Предупреждение о браузере
3. `ThemeToggle.vue` - Переключатель темы
4. `ToastCenter.vue` - Центр уведомлений
5. `TopbarProgress.vue` - Прогресс-бар
6. `AppBreadcrumbs.vue` - Хлебные крошки
7. `NotificationContainer.vue` - Контейнер уведомлений
8. `NotificationItem.vue` - Элемент уведомления

#### Специализированные компоненты (7):
1. `SmartUnitValue.vue` - Умное отображение единиц
2. `UnitValue.vue` - Отображение единиц
3. `FileInput.vue` - Загрузка файлов
4. `GeolocationPicker.vue` - Выбор геолокации
5. `ChartContainer.vue` - Контейнер графиков
6. `MobileCard.vue` - Базовая мобильная карточка
7. `Notifications.vue` - Система уведомлений

---

## 🗄️ АКТУАЛЬНЫЕ STORES (16 STORES)

### Stores через createBaseStore (НЕ вызывать с `()`):

1. **usePurchasesStore** - Управление закупками
   - Базовый CRUD
   - Фильтрация по объектам, датам, ответственным
   - Статистика по закупкам

2. **useUnitsStore** - Управление единицами измерения
   - Базовый CRUD
   - Список единиц для селектов

3. **useObjectsStore** - Управление объектами
   - Базовый CRUD
   - Фильтрация по ответственным
   - Геолокация

4. **useEmployeesStore** - Управление сотрудниками
   - Базовый CRUD
   - Фильтрация по ролям
   - Назначение на объекты

5. **useWriteOffsStore** - Управление списаниями
   - Базовый CRUD
   - Фильтрация по объектам, материалам, этапам
   - Статистика по этапам

6. **useStockSnapshotsStore** - Управление движениями остатков
   - Базовый CRUD
   - Журнал движений
   - Фильтрация по источникам

7. **useSuppliersStore** - Управление поставщиками
   - Базовый CRUD
   - Поиск поставщиков

8. **useMaterialsStore** - Управление материалами
   - Базовый CRUD + расширенные фильтры
   - Фильтрация по категориям, SKU, названию
   - Загрузка фото
   - **getMaterialsByObject** - получение материалов по объекту

9. **useMaterialCategoriesStore** - Управление категориями
   - Базовый CRUD
   - Иерархическая структура

10. **useBalancesStore** - Управление остатками
    - Специфичная обработка API
    - Flattening вложенных данных
    - Фильтрация по объектам и датам

11. **useArchiveStore** - Управление архивными периодами
    - Закрытие/открытие периодов
    - Фильтрация по месяцам
    - Валидация прав доступа

### Stores через defineStore (вызывать с `()`):

12. **useAuthStore** - Аутентификация
    - Login/logout
    - Обновление токенов
    - Профиль пользователя

13. **useThemeStore** - Управление темами
    - Светлая/темная тема
    - Системная тема

14. **useNotificationsStore** - Уведомления
    - Toast уведомления
    - История уведомлений

15. **useUiStore** - UI состояние
    - Sidebar состояние
    - Модальные окна
    - Глобальное UI состояние

---

## 🎯 КОМПОЗАБЛЫ (11 COMPOSABLES)

1. **useGenericForm** - Универсальные формы
   - Валидация
   - Обработка ошибок
   - Отслеживание изменений

2. **useGenericList** - Универсальные списки
   - Пагинация
   - Сортировка
   - Фильтрация

3. **useAutoFilters** - Автоматические фильтры
   - Debounce поиска
   - Обновление фильтров
   - Сброс фильтров

4. **usePagination** - Пагинация
   - Управление страницами
   - Размер страницы
   - Видимые страницы

5. **useResponsiveTable** - Адаптивные таблицы
   - Определение мобильного устройства
   - Переключение между таблицей и карточками

6. **useErrorHandler** - Обработка ошибок
   - Парсинг ошибок API
   - Вложенные ошибки
   - Toast уведомления

7. **useExport** - Экспорт данных
   - CSV экспорт
   - Excel экспорт
   - PDF экспорт

8. **useLoading** - Индикаторы загрузки
   - Управление состоянием загрузки
   - withLoading обертка

9. **useNotifications** - Уведомления
   - Создание уведомлений
   - Управление уведомлениями

10. **useRouter** - Утилиты роутинга
    - Навигация
    - Middleware

11. **useAnimations** - Анимации
    - Transition эффекты
    - Анимированные компоненты

---

## ✨ СИСТЕМА СПИСАНИЙ (WriteOff)

### Полная реализация
✅ **Backend модель WriteOff** с автоматическим формированием записей в журнале движений
✅ **Frontend форма WriteOffForm** с умным автозаполнением полей
✅ **Упрощенный интерфейс** без validation_warnings
✅ **Фильтрация материалов** по выбранному объекту
✅ **Автозаполнение единиц измерения** из материала
✅ **Автозаполнение ответственного** из объекта
✅ **Отображение актуального остатка** материала

### Удалено из системы:
❌ **validation_warnings** - удалены все предупреждения
❌ **StockValidationService** - удален файл validation.py
❌ **Расчет "После списания"** - убран из UI
❌ **Проверки отрицательных остатков** - упрощена логика

### Автозаполнение полей в WriteOffForm

#### При выборе объекта:
1. Загружаются материалы для этого объекта (API: `/materials/by-object/`)
2. Автоматически выбирается ответственный за объект (если не изменен пользователем)
3. Сбрасываются поля "Материал" и "Единица измерения"

#### При выборе материала:
1. Автоматически подставляется единица измерения из `material.default_unit`
2. Поле "Единица измерения" блокируется, если у материала есть `default_unit`
3. Загружается текущий остаток материала на объекте

#### Отображение остатка:
```vue
<div class="alert alert-info">
  <div class="font-bold">Актуальный остаток материала</div>
  <div class="mt-1">
    <span class="font-mono">{{ currentBalance.toFixed(6) }}</span>
    <span class="ml-1">{{ unitCode }}</span>
  </div>
</div>
```

### Бизнес-логика фильтрации материалов

**API Endpoint:**
```
GET /api/v1/materials/by-object/?object_id=1&is_active=true
```

**Принцип работы:**
1. Материалы фильтруются на основе записей в `PurchaseItem`, где закупка связана с выбранным объектом
2. Показываются только активные материалы (`is_active=true`)
3. Материалы сортируются по названию
4. При смене объекта список обновляется автоматически

**Режим редактирования:**
- Материалы загружаются для объекта из существующей записи
- Если текущий материал недоступен для объекта, он добавляется в список с пометкой "(недоступен для объекта)"

---

## 📱 СТРАНИЦЫ ПРИЛОЖЕНИЯ (25+ PAGES)

### Основные модули:

#### Закупки (Purchases/)
- `List.vue` - Список закупок с фильтрацией
- `PurchaseForm.vue` - Форма создания/редактирования
- `PurchaseInfo.vue` - Просмотр полной информации

#### Материалы (Materials/)
- `List.vue` - Список материалов
- `MaterialForm.vue` - Форма материалов
- `Categories/List.vue` - Категории материалов
- `Categories/CategoryForm.vue` - Форма категории
- `Categories/CategoryInfo.vue` - Информация о категории

#### Объекты (Objects/)
- `List.vue` - Список объектов
- `ObjectForm.vue` - Форма объектов

#### Остатки (Stocks/)
- `List.vue` - Движения остатков
- `Balances.vue` - Остатки по объектам (с сортировкой вложенных таблиц)
- `StockForm.vue` - Форма движения
- `StockSnapshotForm.vue` - Форма snapshot

#### Списания (WriteOffs/)
- `List.vue` - Список списаний
- `WriteOffForm.vue` - Форма списаний с автозаполнением

#### Поставщики (Suppliers/)
- `List.vue` - Список поставщиков
- `SupplierForm.vue` - Форма поставщиков

#### Сотрудники (Employees/)
- `List.vue` - Список сотрудников
- `EmployeeForm.vue` - Форма сотрудников

#### Единицы (Units/)
- `List.vue` - Список единиц измерения
- `UnitForm.vue` - Форма единиц

#### Архив (Archive/)
- `List.vue` - Управление архивными периодами
- `ListSimple.vue` - Упрощенный список
- `ListWorking.vue` - Рабочий список

#### Отчеты (Reports/)
- `ByPeriod.vue` - Отчет по периодам
- `ByObject.vue` - Отчет по объектам
- `ByMaterial.vue` - Отчет по материалам
- `ByResponsible.vue` - Отчет по ответственным

#### Аутентификация:
- `Login.vue` - Страница входа

---

## 🔧 КЛЮЧЕВЫЕ ИЗМЕНЕНИЯ И УЛУЧШЕНИЯ

### 1. Упрощение интерфейса списаний
- ✅ Удалены все validation_warnings
- ✅ Удален файл `stock/validation.py` на backend
- ✅ Удалены функции `get_validation_warnings` и `validate_writeoff`
- ✅ Упрощена форма списаний - только актуальный остаток

### 2. Улучшение UX форм
- ✅ Автозаполнение полей на основе выбранных данных
- ✅ Блокировка полей при автозаполнении
- ✅ Умная фильтрация материалов по объектам
- ✅ Отслеживание пользовательских изменений (`userModifiedFields`)

### 3. Сортировка вложенных таблиц
- ✅ Добавлена сортировка в таблице материалов объекта (Balances.vue)
- ✅ Сортировка по полям: "Материал", "Остаток", "Приход", "Расход"
- ✅ Индикаторы сортировки в заголовках таблицы

### 4. Обработка ошибок
- ✅ Улучшена система парсинга вложенных ошибок
- ✅ Корректное отображение ошибок валидации из API
- ✅ Автоматическая очистка ошибок при изменении полей

### 5. Единообразная пагинация
- ✅ Все stores используют `createBaseStore`
- ✅ Консистентное поведение пагинации
- ✅ Единый интерфейс фильтрации

---

## 📝 ТИПЫ И API

### Типы списаний (stocks.ts)

```typescript
export interface WriteOff {
  id: number;
  date: string;
  object: number;
  object_name: string;
  material: number;
  material_name: string;
  unit: number;
  unit_code: string;
  quantity: string;
  stage: Stage;
  responsible: number;
  responsible_name?: string;
  comment?: string;
  is_archived: boolean;
  current_balance: string; // Актуальный остаток
  smart_quantity: SmartQuantity;
  created_at: string;
  updated_at: string;
}

export interface WriteOffCreateRequest {
  date: string;
  object: number;
  material: number | null; // nullable
  unit: number;
  quantity: string;
  stage: Stage;
  responsible: number;
  comment?: string;
}
```

### API Endpoints для списаний

```
GET    /api/v1/writeoffs/              - Список списаний
POST   /api/v1/writeoffs/              - Создание списания
GET    /api/v1/writeoffs/{id}/         - Получение списания
PUT    /api/v1/writeoffs/{id}/         - Обновление списания
DELETE /api/v1/writeoffs/{id}/         - Удаление списания

GET    /api/v1/materials/by-object/    - Материалы по объекту
GET    /api/v1/stock/snapshots/balance/ - Текущий остаток
```

---

## 🎯 РЕКОМЕНДАЦИИ

### Для разработчиков:
1. Используйте `createBaseStore` для новых stores
2. Следуйте паттерну автозаполнения полей из `WriteOffForm`
3. Используйте `useErrorHandler` для обработки ошибок API
4. Применяйте Daisy UI стили для консистентного UI

### Для пользователей:
1. Система списаний упрощена - просто указывайте количество
2. Единица измерения подбирается автоматически
3. Текущий остаток отображается для справки
4. Все предупреждения удалены для упрощения работы

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

**Статус проекта**: ✅ Полностью функционален
**Версия**: 3.0
**Готовность**: 95%
**Осталось реализовать**: 
- PDF экспорт (5%)
- WebSocket уведомления (планируется)
- PWA поддержка (планируется)

**Проект готов к продакшену! 🚀**

---

Обновлено: 08 Октября 2025
Следующее обновление: по мере необходимости


