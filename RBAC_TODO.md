# RBAC - Оставшиеся задачи

## Frontend

### ✅ Выполнено
- [x] Миграция отображения ролей в компонентах (List.vue, EmployeeCard.vue, AppSidebar.vue)
- [x] Удаление `meta.roles` из всех маршрутов
- [x] Обновление роутинга для использования только `meta.permissions`
- [x] Обновление функций в `stores/employees.ts` с fallback на RBAC
- [x] Удаление зависимости от `authStore.role` в навигации

### ✅ Выполнено (дополнительно)
- [x] Удалены устаревшие функции из `types/router.ts` (ROLE_HIERARCHY, hasRoleAccess, getAccessibleRoutes)
- [x] Удален `middleware.roles()` из `router/middleware.ts`
- [x] Удалены deprecated функции из `router/middleware.ts` (hasRequiredRole, getAccessibleRoutes)
- [x] Обновлены тесты в `__tests__/middleware.test.ts` - удалены тесты для roles middleware

### ⚠️ Требует внимания (низкий приоритет)

#### 1. Fallback на legacy роли в `stores/employees.ts`
- Функции `getBrigadierOptions()`, `getResponsibleEmployees()`, `canBeResponsible()`, `getByRole()` имеют fallback на `item.role`

**Статус**: Необходим для обратной совместимости с backend
**Действие**: Удалить fallback после миграции backend (когда backend перестанет возвращать `role`)

### 📝 Опционально (можно оставить)

#### 1. Типы в `api/types/employees.ts`
- Поле `role?: UserRole` помечено как DEPRECATED, но оставлено для обратной совместимости
- Поле `roles?: Array<{...}>` - новое RBAC поле

**Статус**: Корректно - оставлено для совместимости с API
**Действие**: Удалить после миграции backend

## Backend

### 🔴 Критично (требует реализации)

#### 1. Миграция данных
- [ ] Создать команду `migrate_legacy_roles_to_rbac.py`
- [ ] Мигрировать все legacy роли пользователей в RBAC
- [ ] Проверить, что все пользователи имеют RBAC роли

#### 2. Обновление сериализаторов
- [ ] Убедиться, что все сериализаторы возвращают `roles` (RBAC)
- [ ] Поле `role` оставить только для чтения (read_only=True)

#### 3. Автоматическая миграция
- [ ] Добавить сигнал для автоматической миграции при сохранении пользователя
- [ ] Создать UserRole при установке legacy роли

#### 4. Фильтрация и поиск
- [ ] Обновить фильтры для работы с RBAC ролями
- [ ] Добавить фильтрацию по `roles` вместо `role`

### ⚠️ После миграции данных

#### 5. Удаление legacy кода
- [ ] Удалить поле `role` из модели User (миграция Django)
- [ ] Удалить все проверки `user.role == 'admin'`
- [ ] Заменить на проверки через permissions

#### 6. Обновление документации
- [ ] Обновить API документацию
- [ ] Удалить упоминания legacy ролей

## Приоритеты

### Высокий приоритет (сделать сейчас)
1. ✅ Frontend миграция - **ВЫПОЛНЕНО**
2. 🔴 Backend: Миграция данных - **ТРЕБУЕТ РЕАЛИЗАЦИИ**
3. 🔴 Backend: Обновление сериализаторов - **ТРЕБУЕТ РЕАЛИЗАЦИИ**

### Средний приоритет (после миграции backend)
4. Обновление тестов frontend
5. Удаление устаревших функций из `types/router.ts`
6. Удаление middleware для roles

### Низкий приоритет (после полной миграции)
7. Удаление fallback'ов в `stores/employees.ts`
8. Удаление поля `role` из типов frontend
9. Финальная очистка кода

## Рекомендации

1. **Сначала backend**: Реализовать миграцию данных и обновить API
2. **Затем frontend**: Удалить все fallback'и и устаревший код
3. **Тестирование**: Протестировать на тестовой базе перед production
4. **Мониторинг**: Отслеживать ошибки после миграции

## Текущий статус

- ✅ **Frontend**: 98% готов - все устаревшие функции удалены, остались только fallback'и для совместимости с backend
- ⚠️ **Backend**: 0% готов - требуется полная реализация миграции

### Выполнено в последнем обновлении:
- ✅ Удалены все deprecated функции из `types/router.ts`
- ✅ Удален `middleware.roles()` и связанные функции
- ✅ Обновлены тесты для работы с RBAC permissions
- ✅ Улучшена обработка ошибок API (parseApiError для всех ошибок)
- ✅ Исправлена обработка unique constraint ошибок при назначении ролей
